name: chirp_addons_example
description: Runnable Flutter example app for chirp_addons.
publish_to: 'none'

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  chirp: ^0.9.0
  chirp_addons:
    path: ../
  dio: ^5.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
